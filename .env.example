# Copy to .env (do not commit .env)

# OpenAI (required for PMC OA extraction + Phase C labeling)
OPENAI_API_KEY="REPLACE_ME"
# OPENAI_BASE_URL="https://api.openai.com/v1"  # optional

# NCBI E-utilities (recommended for PMC + PubMed)
NCBI_EMAIL="REPLACE_ME"
# NCBI_API_KEY="REPLACE_ME"  # optional (higher rate limits)
NCBI_TOOL="paperjn"
